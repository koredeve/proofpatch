{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "echo 'serverless-only: no build step'",
  "outputDirectory": ".",
  "functions": {
    "api/index.js": { "maxDuration": 60 }
  },
  "rewrites": [{ "source": "/(.*)", "destination": "/api/index" }]
}
